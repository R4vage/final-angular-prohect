import { SecondTrackMusicPipe } from '../second-track-music.pipe';

describe('SecondTrackMusicPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondTrackMusicPipe();
    expect(pipe).toBeTruthy();
  });
});
