import { AlbumTotalDurationPipe } from '../album-total-duration.pipe';

describe('AlbumTotalDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumTotalDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
