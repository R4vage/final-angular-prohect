import { TitleCardTruncatePipe } from './title-card-truncate.pipe';

describe('TitleCardTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCardTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
