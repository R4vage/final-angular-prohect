import { PlaylistToMusicPipe } from './playlist-to-music.pipe';

describe('PlaylistToMusicPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistToMusicPipe();
    expect(pipe).toBeTruthy();
  });
});
