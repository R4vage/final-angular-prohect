import { CategoryToMusicPipe } from './category-to-music.pipe';

describe('CategoryToMusicPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToMusicPipe();
    expect(pipe).toBeTruthy();
  });
});
